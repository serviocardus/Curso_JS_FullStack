let arr = [];

for (let i = 0; i < 5; i++){
  arr[i] = prompt("Informe o nome:")
  console.log(arr[i]);
}
console.log(arr);

// Array - Estrutura de dados para guardar vários valores

const arrMix = [
    '3way Academy',
    250,
    true,
    { country: 'Finland', city: 'Helsinki' },
    { skills: ['HTML', 'CSS', 'JS', 'React',
   'Python'] }
   ]
   
   console.log(arrMix[3]);